services:
  - type: web
    name: deadmag-server-eu
    runtime: node
    plan: free
    region: frankfurt
    autoDeploy: true
    branch: main
    buildCommand: npm install --omit=dev
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 20
    healthCheckPath: /
